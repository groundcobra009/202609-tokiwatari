# DECISIONS

このプロジェクトで**確定した判断**の記録。採用した案だけでなく、**採用しなかった案とその理由**を必ず残す。
同じ議論を繰り返さないための資産であり、次に作業するAI・人間が最初に読む場所（役割の使い分けは ai-company の CLAUDE.md §4.6）。

## 使い方

- **提案の前に読む**。新しい設計・技術・ツール・構成を提案する前に、このファイルを確認する
- **再提案するときは明示する**。過去に不採用となった案を再び提案する場合は、①前回の不採用理由 ②今回再提案する理由 ③前回から変化した条件 を必ず書く。条件が変わっていないなら再提案しない
- **新しいエントリは末尾に追記する**（`CHANGELOG.md` は新しい順に上へ積むが、こちらは**逆**。判断の履歴は時系列で読むため）
- **過去のエントリは書き換えない**。決定を変えるときは新しいエントリを立て、旧エントリの `状態` だけを `変更済み（DEC-NNN で変更）` に更新する
- 番号は `DEC-001` から連番。欠番を埋め直さない

## 書くもの・書かないもの

**書く**（後から「なぜ？」と聞かれるもの）

- アーキテクチャ・技術選定
- コスト・課金に関わる判断
- セキュリティ・権限・個人情報の扱い
- プロジェクト固有の制約から来た判断
- 一般論としては良いが、この案件では使えない・使わないと決めたもの

**書かない**（肥大化を防ぐ）

- 変数名・表現・軽微な実装詳細（Issueとコミット履歴に残る）
- まだ決まっていない検討中の話（それはIssueで議論する）

## 記録フォーマット

- **日付**: YYYY-MM-DD
- **Issue**: #NN（あれば）
- **決定内容**: 何を決めたか
- **決定理由**: なぜそう決めたか
- **検討した代替案**: 何を検討して採用しなかったか。**案ごとに不採用理由まで書く**
- **再検討条件**: どの条件が変われば再検討するか（`状態` が `条件付きで再検討可` のときは必須）
- **状態**: `有効` / `条件付きで再検討可` / `変更済み（DEC-NNN で変更）`

`状態` を必ず書くのは、**不採用を永久ルールとして固定させないため**。「一度不採用になったから絶対に使ってはいけない」と読まれると改善が止まる。

---


---

## DEC-001 技術スタックは Next.js＋OpenNext＋Cloudflare Workers＋KV＋Claude API

- **日付**: 2026-09-13
- **Issue**: #1
- **決定内容**: フロント/API は Next.js、ホスティングは Cloudflare Workers（OpenNext）、データは Cloudflare KV、AI は Claude API（claude-sonnet-5）。公開URLは `tokiwatari.keitro-aigc.com`（カスタムドメイン・workers.dev不使用）
- **決定理由**: ハッカソン当日（残り8時間）に、この環境で実績のある構成（`001Area/keitaro-materials`）をそのまま流用できる。ハマりどころが少ない
- **検討した代替案**:
  - Vite＋React＋Hono Worker: 軽量だがこの環境での実績が少なく、当日の初期化コストが読めない → 不採用
  - ローカルデモのみ（Next.js＋SQLite）: デプロイ工程を削れるが、審査員・チームが自分の端末で触れない → 不採用
  - Vercel: Claude実行環境から本番デプロイが分類器でブロックされる実績あり → 不採用
- **再検討条件**: ハッカソン後に継続開発する場合、D1やDurable Objectsへの移行を検討
- **状態**: 有効

## DEC-002 デモ用の過去の思考の種は「本人の既存発信の取り込み」

- **日付**: 2026-09-13
- **Issue**: #1
- **決定内容**: 「過去の思考」はサービス内投稿だけでなく、本人がすでに持つ発信（X投稿など）を取り込んで作る。当日のデモ種は @keitaro_aigc のX投稿をAPI取得したJSON（未確定。間に合わなければチーム各自の手書き種に切り替える）。取り込みUIは非スコープ（事前投入）
- **決定理由**: サービス開始時点で過去側が空だと体験が成立しない。本人の日付付きの言葉がそのまま「当時の人格」の材料になる
- **検討した代替案**:
  - サービス内の投稿だけ: 過去側が育つまで体験が成立しない → 不採用
  - 架空ユーザーのAI生成データ: 外部依存ゼロだがリアルさが落ち、ピッチの説得力が下がる → 不採用（フォールバックとしては可）
- **再検討条件**: X API の取得制限・認証で当日中に取れない場合は手書き種へ
- **状態**: 変更済み（DEC-006 で変更）

## DEC-003 AI返答はキー未設定時に決定的モックへ切り替え、応答に `mode` を含める

- **日付**: 2026-09-13
- **Issue**: #1
- **決定内容**: `ANTHROPIC_API_KEY` が無い、または `MOCK_AI=1` のときは Claude API を呼ばず、対象投稿の年月・文脈投稿の一部を埋め込んだ決定的なモック文を返す。`/api/reply`・`/api/post` の応答と smoke の出力に `mode: "live" | "mock"` を含め、どちらで動いたかを常に見えるようにする
- **決定理由**: AIはAPIキーを扱わない（登録は社長）ため、キー無しでもゴールコマンド `npm run check` が完走する経路が必要。モードを明示しないと「動いた」の意味が曖昧になり、デモ本番で実APIが入っていないことに気づけない
- **検討した代替案**:
  - smoke 内でAI呼び出しをスキップする → 返答の保存・スレッド表示・`aiGenerated`/`at` の検証ができなくなる → 不採用
  - ランダム文のモック → 再現性が無く、失敗時の切り分けができない → 不採用
- **再検討条件**: 本番運用でモック経路が不要になったら削除してよい（ただし smoke はキー無し環境で回る必要がある）
- **状態**: 有効

## DEC-004 KVレイアウトは `post:<id>` ＋ `index:all` / `index:<author>`、Workers外はメモリにフォールバック

- **日付**: 2026-09-13
- **Issue**: #1
- **決定内容**: 投稿は `post:<id>` に1件ずつ、並び順の元になる id 配列を `index:all` と `index:<author>` に持つ。タイムラインは index を読んで各投稿を並列取得し、サーバー側で `at` 昇順に並べる。KV binding が無い環境（`next build` の静的生成や単体実行）ではプロセス内メモリに保存する
- **決定理由**: ハッカソン規模（数十〜数百件）なら十分で、追加の1件書き込みが index の read-modify-write だけで済む。`next dev` でも `initOpenNextCloudflareForDev()` 経由でローカル miniflare KV（`.wrangler/state/`）が使えるため、開発と本番で経路が変わらない
- **検討した代替案**:
  - 全投稿を1つのJSON値に持つ → 読みは1回で済むが、返信のたびに全件を書き戻すことになり、同時書き込みで消える → 不採用
  - D1（SQLite）→ 範囲クエリは自然だがマイグレーション・バインディングの初期化が当日の時間に見合わない → 不採用（DEC-001 の再検討条件に含む）
- **再検討条件**: 投稿が数千件を超える、または同時書き込みが増えたら D1 / Durable Objects へ
- **状態**: 有効

## DEC-005 時間越しの返信は「元投稿と同じ時間座標」に置く

- **日付**: 2026-09-13
- **Issue**: #1
- **決定内容**: 過去投稿への人間の返信も、AI再現の返答も `at` は元投稿と同じにし、`replyTo` でスレッド化する。人間の返信は `kind: "now"`・`createdAt` に実際の送信時刻を持ち、画面には「YYYY年M月D日 に送信」と表示する。未来宛て投稿への AI推定返答は未来日付の `at` を持つ
- **決定理由**: 「その時点の本人と会話した」記録がその時間座標に残ることが体験の核。返信を「今」の位置に置くとスレッドが時間軸上でばらける
- **検討した代替案**:
  - 返信の `at` を送信時刻にする → タイムライン上で元投稿から離れ、会話として読めない → 不採用
- **再検討条件**: 「未来から届いた返信を今の位置にも通知する」機能を足すときは、表示側で二重に載せる（データは変えない）
- **状態**: 有効

## DEC-006 種データはAPI取り込みをせず、手書き種＋「取り込み」入口のUIだけ用意する

- **日付**: 2026-09-13
- **Issue**: #3
- **決定内容**: ハッカソンのデモでは X 投稿を API で取り込まない。種は `seed/sample.json`（手書き・3人分）を使う。「自分の過去の発信を取り込む」機能は**入口（ボタン・説明・準備中表示）だけ**を置き、実装はしない
- **決定理由**: 社長判断（2026-09-13 12:15）。デモでありハッカソンなので、取り込みの実装より「動くこと」（RUBRIC 02 実現性・同点時の最優先）に時間を使う。企画上は「既存の発信を取り込める」ことが価値なので、入口だけ見せて展開性（05）で語る
- **検討した代替案**:
  - xapi で @keitaro_aigc の投稿を取得して種にする（DEC-002 の当初案）: リアルだが当日中の認証・整形に時間がかかる → 不採用
  - 取り込みUIも作らない: 展開性の説明材料が減る → 不採用（入口だけ置く）
- **再検討条件**: ハッカソン後に継続開発する場合、X アーカイブ（公式エクスポート）の取り込みを最初のIssueにする
- **状態**: 有効（DEC-002 の「当日はX取得」部分を本エントリで変更）


## DEC-007 時限公開は存在だけ見せる

- **日付**: 2026-09-13
- **Issue**: #3（Beads Epic tw-n2d）
- **決定内容**: `visibility` は public / friends / private（旧データ・未指定は public）、`unlockAt` は任意のISO日時。本人以外は解禁前の本文を取得できず、`text: ""`・`locked: true`・解禁日時だけを持つカードとして表示する。private/friends の閲覧条件も同時に満たす必要があり、時刻到達だけで private が public に変わるわけではない。`locked` は保存しない。投稿・返信・種投入で公開設定を検証し、AI返答も制限を継承する。返信は親より公開範囲を広げず、未閲覧の投稿への返信は403。生成文脈は公開・解禁済みの投稿と返信対象に限定し、別の非公開投稿をAI経由で漏らさない。
- **決定理由**: 存在が見えること自体が「未来に開く思考」の体験になる。画面だけを隠してAPIに本文を残す実装では要件を満たせない。
- **検討した代替案**:
  - 未解禁の投稿を一覧から完全に除く: タイムカプセルがあることを体験できないため不採用。
  - クライアントだけで伏字にする: 通信内容から本文を読めるため不採用。
  - 非公開の履歴も公開AI返信の材料にする: 生成文から漏れる可能性があるため不採用。本人向けの文脈拡張は別途扱う。
- **再検討条件**: 継続開発で非公開スレッドの参加者権限や本人限定のAI文脈を設計するとき。
- **状態**: 有効

## DEC-008 friends は今日は viewer あり＝閲覧可（フォロー未実装）

- **日付**: 2026-09-13
- **Issue**: #3
- **決定内容**: friends は空でない viewer を閲覧可とする。解禁前は本人以外に本文を返さない。ゲストモードでは `?viewer=<author>` と「わたし」の切替で3人分の体験を確認できる。これはデモ用の自己申告であり、本人確認ではない。認証設定が揃った場合は署名済みセッションのユーザーIDを使用し、クエリの viewer や投稿bodyの author/authorName で他人を名乗れない。設定済みの未ログインアクセスは空の viewer として扱う。
- **決定理由**: 当日のフォロー機能は非スコープ。公開範囲の違いが動くことを優先する（RUBRIC 02）。
- **検討した代替案**:
  - フォロー関係を永続化して相互フォローを判定: データモデルとUIの追加が今回の範囲を超えるため不採用。
  - friends を public と同じ扱いにする: 匿名との差がなくなるため不採用。
- **再検討条件**: デモを超えて実際の非公開情報を扱う前。全アクセスの認証必須化・ゲスト共有IDの解消・友人関係の定義を別Issueで行う。
- **状態**: 条件付きで再検討可

## DEC-009 認証は環境変数があるときだけ有効・ゲストモードで動作

- **日付**: 2026-09-13
- **Issue**: #3
- **決定内容**: WorkOS AuthKitを導入し、`WORKOS_API_KEY`・`WORKOS_CLIENT_ID`・`WORKOS_COOKIE_PASSWORD`・`NEXT_PUBLIC_WORKOS_REDIRECT_URI` が揃ったときだけ認証を有効にする。不足時は `{ id: "guest", name: "ゲスト" }`。Cookie暗号鍵は32文字以上。Workers bindingsをSDKのロード前に渡し、各APIで公式の `authkit()` を使ってセッションを検証する。内部セッションヘッダーは返さず、SDKが選別したレスポンスヘッダーだけ反映する。ログイン・callback・POSTログアウトを実装し、Google OAuthはWorkOSのAuthKit画面経由で開始する。公開名義の制約に従い、このデモのログイン時表示名は「けいたろう」とし、Googleの氏名・メールアドレスを投稿や画面に出さない。既存種の作者IDとWorkOS IDは自動統合しない。
- **決定理由**: APIキーをCodexが登録せずとも既存体験とゴールコマンドが動き続ける必要がある。設定済みなのに認証検証に失敗した場合は503で止め、ゲストへの暗黙の切替で権限を緩めない。
- **検討した代替案**:
  - Googleログインを全環境で必須にする: キーなし検証と当日デモが止まるため不採用。
  - Googleプロフィールをそのまま公開名にする: 本名・メールを公開する可能性があるため不採用。継続版では公開用ニックネームを別途設定する。
  - WorkOS IDを既存のkeitaroに自動で結びつける: 所有者の確認なしで過去の非公開投稿を渡すことになるため不採用。
- **検証**: `npm run check` exit 0（mock / guest）。ビルド済みローカルWorkersでもsmoke全ケース成功。ダミーの環境設定では、認証有効判定・PKCE付きログインURL生成・viewer/author自己申告の無視を検証（外部遷移なし）。実Googleログイン、セッション更新、ログアウトの実サービス連携とClaude実応答は実キー未設定のため未検証。
- **けいたろうの確認事項**: WorkOS側のGoogleプロバイダー有効化、callback URL（`/auth/callback`）とlogout後URL（`/`）の登録、実キー設定後のGoogleログイン→投稿→ログアウト。ゲストモードのままならデモ用の手書きデータのみを使う。既存の `/api/seed` はデモ投入用であり、本運用の管理者認可は今回の非スコープ。
- **再検討条件**: ハッカソン後に複数利用者向けサービスとして継続する場合。公開ニックネーム・種データの本人移行・管理者認可を設計する。
- **状態**: 条件付きで再検討可

## DEC-010 Apple風の画面とデモの明示

- **日付**: 2026-09-13
- **Beads**: tw-afy
- **決定内容**: ユーザー指定の[Apple DESIGN.md](https://github.com/kzhrknt/awesome-design-md-jp/blob/main/design-md/apple/DESIGN.md)を参照し、白・薄灰・青の操作ボタン、余白、丸み、控えめな透過ヘッダーを採用。過去は青、未来は紫で区別する。日本語を含む端末のシステムフォントを使い、外部フォントを取得しない。冒頭に3体験の入口を置き、ゲスト時には手書きサンプルと3人の体験ユーザーを明示する。
- **決定理由**: 初見でも体験の目的とデモデータであることが伝わるようにする。最新のアプリ完成目標は13:30。
- **検証**: 改修後のnpm run check exit 0（mock/guest）。ローカルWorkersでPC幅1280・スマートフォン幅390、体験開始・テーマ差し込み・ユーザー切替・取り込みモーダルを確認。
- **状態**: 有効。公式審査の独立したデザイン点は設けず、価値・実現性の証拠として扱う。

## DEC-011 AI文脈は対象時点までの記録に限定

- **日付**: 2026-09-13
- **Beads**: tw-wel（ユーザーの評価改善ループ指示）
- **決定内容**: 過去への返信には同じ作者の対象日時以前の公開・解禁済みの元投稿を使い、対象そのものも含める。直近90日で不足する場合も過去だけを探索し、最大10件とする。未来への返信は現在以前の履歴だけを使用し、別の未来宛て投稿を既に起きた実績として扱わない。AI生成文・返信・別の非公開投稿は文脈から除外。投稿内の命令や未来情報を事実として採用しないよう指示する。
- **理由**: 「当時の視点」という体験と、実際にAIへ渡す記録の時間境界を一致させる。プロンプトだけに頼らず入力選定で防ぐ。
- **運用**: 本文は2000文字以内、入力型違反は400。AI呼び出しは20秒タイムアウト・自動再試行なし。過去返信はAI成功前には保存しない。未来投稿は保存後にAI生成するため、AI失敗時は本文保存済みと説明する。
- **検証**: npm run check exit 0（mock/guest）。直後の未来記録・疎な履歴・最初の投稿・非公開・未解禁・別人・AI生成文・未来宛ての再利用の反例をsmokeへ追加。実AIの口調やモデルの事前知識まで保証するものではない。
- **状態**: 有効。

## DEC-012 サービス名をタイムトークに変更

- **日付**: 2026-09-13
- **Beads**: tw-45f
- **決定内容**: ユーザー指定の公開サービス名を「タイムトーク」、英語補助表記を「Time Talk」とする。時計・会話の吹き出し・時間の循環を組み合わせた青と紫のロゴを作成し、public/timetalk-logo.pngに保存。画面、ブラウザータイトルとアイコン、README、発表原稿、デモ台本に反映。
- **理由**: ユーザーによる改名。会話の体験が伝わる名称とロゴへ統一。
- **範囲**: インフラ識別子・ドメイン・ディレクトリ名は変更しない。GOAL.md・docs/proposal.md・既存の決定履歴は旧称の資料として保持。本エントリが改名の根拠となる。
- **検証**: npm run check exit 0（mock/guest）。ローカルWorkersで新名称・ロゴ・390px幅、過去と未来の質問例→送信→返答を確認。ブラウザーエラーログなし。
- **状態**: 有効。

## DEC-013 文字主体ロゴと今回のCloudflare公開

- **日付**: 2026-09-13
- **Beads**: tw-4oz
- **決定内容**: ユーザーの再制作指示と「文字主体＋小さな抽象マーク」の選択により、黒い文字と黒/青のずれた対話記号に変更。public/timetalk-mark.svgとtimetalk-wordmark.svgを作成。旧PNGは_archive/brandingへ保管。
- **公開の許可**: 最新のユーザー指示「Cloudflareでデプロイまでいきましょう」を、今回の本番公開に対する明示的な許可として適用。過去のデプロイ禁止との矛盾は、この依頼に限り最新指示を優先する。git commit/push/gh、キーの表示・登録は行わない。
- **AI設定**: Claude APIキーは本人が後からWorker tokiwatariのSettings → Variables and SecretsにSecretとして登録する。未設定中はmock/guestで動く。READMEに設定・再確認手順を記録。
- **状態**: 有効。デプロイの実行結果はCHANGELOGと自己評価に別途記録する。

## DEC-014 初回の日時をサーバーと画面で共有

- **日付**: 2026-09-13
- **決定内容**: 初回描画用の日時をページからTimelineへ渡す。表示用の日本語日時はブラウザー起動後に描画し、未来の初期日付は同じ基準時刻からUTCで算出する。
- **理由**: 公開版の実操作でReact #418（サーバーと画面の初回テキスト不一致）を検出。サーバー・ブラウザーが別々に現在時刻と書式を決める経路を解消する。
- **状態**: 有効。checkと再公開後のブラウザーログで検証する。

## DEC-015 投稿・会話中心のフィードへ変更

- **日付**: 2026-09-13
- **Beads**: tw-3sa
- **決定内容**: ユーザーのXに近い構成への改善指示と継続指示を受け、大きな紹介エリアを縮め、投稿欄を先頭に置く。白基調と新ロゴは維持。過去と今・未来・すべての表示切替、投稿後の該当フィード表示、連続したカード、作者の頭文字アイコンを採用。年ナビは投稿がある年と現在・10年後を表示する。テーマ・取り込みは折りたたみから利用する。
- **検証**: check exit 0（mock/guest）、ローカルWorkersで切替・年ジャンプ・過去返信・未来投稿・テーマ差し込み・390px幅を確認。
- **関連修正**: 画面検証でデモreset後に古い作者別履歴が文脈へ残ることを発見。listPostsは全体の有効インデックスと照合し、古い履歴を除外。smokeに再発検証を追加。保存済みファイルは削除しない。
- **状態**: 有効。APIキーなしの操作性を改善したもので、実AI品質や利用者評価の確認済み宣言ではない。

## DEC-016 公開seed APIを閉じ、リセット後の参照を統一

- **日付**: 2026-09-13
- **Beads**: tw-vvi
- **決定内容**: デモデータ投入APIはlocalhost・ループバックだけで利用できる。公開URLではbody処理より前に403を返し、データの上書き・初期化を拒否する。公開済みの種データは維持する。getPostは全体インデックスにないIDを存在しないものとして扱い、reset後の旧投稿への直接返信を拒否する。
- **理由**: 公開環境で任意の初期化ができる状態と、一覧に存在しない旧投稿への返信経路を解消するため。
- **検証**: check exit 0（mock/guest）。ローカルseed成功、reset後に作者別一覧が空かつ旧投稿への返信404を確認。本番403はデプロイ後に確認する。
- **限界**: ゲストのviewerは依然として自己申告で、友人関係や本運用向けの本人確認は保証しない。実在の秘密情報を扱う認証基盤に置き換えたという決定ではない。
- **状態**: 有効。将来の本番データ投入は管理者認可を備えた経路を別途設計する。

## DEC-017 当日の審査から逸脱した採点条件を是正

- **日付**: 2026-09-13
- **Beads**: tw-mvd
- **決定内容**: ユーザー指摘に基づき、公式PDF p.4の5観点・各1〜5点・同点時の実現性→価値を正本とする。Codexが追加した複数利用者の継続意向・導入確約・厳密な比較実験を満点の必須条件とするルールは撤回し、当日の作品・デモ・発表を対象とする採点目安へ修正した。
- **理由**: ハッカソンの審査と長期の事業検証を混同していた。公式の詳細な点数別アンカーはPDFにないため、修正版の段階表も主催者公式ではなく説明用の解釈と明記する。
- **評価**: 修正した尺度で22/25。旧18点との差は尺度の変更であり、実装改善の増分ではない。弱点は価値・新規性・AIの工夫の伝わり方。機械的に25点へ合わせない。
- **状態**: 有効。旧RUBRICは_archive/rubric/long-term-evidence-v1.mdに保存、旧評価は履歴として保持。


## DEC-018 — 時間ごとの返答比較と生成時の参照記録（2026-09-13）

- **背景**: 当日の公式5観点に沿う22/25の評価で、価値・時間による違い・AIの入力と出力の関係が画面だけでは伝わりにくかった。ユーザーは作業期限を15:00へ延長。
- **決定**: 同じ問いを過去と未来へ送る導線、明示的なサンプル選択、2つの返答を並べる比較表示を追加。生成時に渡した投稿ID一覧とAIモードを返答に保存し、「AIに渡した記録」から本文を確認・元投稿へ戻れるようにする。
- **正確性**: 一覧は入力した文脈であり、モデルが実際に引用・依拠したことの証明ではない。旧返答は一覧未保存と明示。履歴0件も明示する。
- **公開制御**: 鍵付きの返答は参照ID一覧も応答から除外。画面には現在閲覧できる投稿のみ表示し、本文の複製保存は行わない。
- **AI**: 初回live比較で未来の実績らしい表現と記録にない経緯が残ったため、過去の具体的な経緯の補完を禁止。未来は原文の短い引用→明示した仮定→今日の行動という順へ修正。比較後は「今日の一歩を自分の言葉で残す」から今日の日付の通常投稿へ戻れる。決断は本人が書く。
- **制限**: 比較表示は同じ画面で生成したペアを保持する。再読み込み後は通常のスレッドで返答を確認できるが、比較ペアの自動復元は行わない。ゲストは共有サンプル用であり、実Googleログインと本人データ運用は未検証。
- **検証**: npm run check exit 0（mock/guest、10群）。参照IDの時間境界・保存・鍵付き時の除外・履歴0件を追加。ローカルWorkersの往復UIと390px幅・横はみ出しなし・console errorなしを確認。


## DEC-019 — 投稿の目的と公開設定を分ける

- **背景**: ユーザーから、入力欄が込み入り、公開時期と宛先の日付の関係が初見で分かりにくいとの指摘。
- **決定**: 初期表示は「今の記録」。本文と「投稿する」を中心にする。「未来の自分に聞く」を選んだ場合だけ対象年と変更欄を表示し、返答は今生成されると説明。公開範囲/時期は独立した「公開設定」へまとめ、現在値は閉じた状態でも表示する。
- **動作**: 今の記録はAPIにatを指定せず、送信時刻で保存。未来モードの日時が過去なら送信を止め、モード切替を案内する。既存の比較導線は未来モード、今日の一歩は今の記録モードへ切り替える。
- **配置**: 比較の使い方・サンプルユーザー変更は折りたたみ。投稿の表示フィルタはフィード上へ移動。公開制御のAPI仕様は維持。
- **修正**: 通常投稿が画面を開いた時刻より後になることで未来側へ分類される表示不具合を修正。保存後に現在時刻を更新し、投稿種別で表示先を選ぶ。
- **検証**: npm run check exit 0（mock/guest、smoke 10群）。ローカルWorkersで通常投稿・未来の質問とモック返答・公開時期の独立操作、390px幅で横はみ出しなし・console errorなしを確認。公開URLで新UIの反映を確認。
- **公開版**: 42ba304f-0fd8-4ebb-bf72-11bfadecf910。今回のAI送信操作はmockで検証し、liveの再採点は行っていない。
